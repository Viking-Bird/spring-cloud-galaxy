server:
  port: 8980

spring:
  application:
    name: pig-consul
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        port: ${server.port}
        enabled: true
        register: true
        deregister: true
        prefer-ip-address: true
        health-check-interval: 15s
        health-check-path: /actuator/health
        instance-id: ${spring.application.name}:${spring.cloud.client.ip-address}:${server.port}
      config:
        enabled: true
        format: yaml
        watch:
          enabled: true
          delay: 1000
          wait-time: 60
        fail-fast: true

globalPrefix: ${spring.application.name}
